{
  "type" : "object",
  "id" : "urn:jsonschema:io:gravitee:policy:messagefiltering:configuration:MessageFilteringPolicyConfiguration",
  "properties" : {
    "filter" : {
      "title": "Filter condition",
      "description": "The condition to apply to filter messages (supports EL).",
      "type" : "string"
    }
  },
  "required": [
    "filter"
  ]
}
